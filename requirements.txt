# 系统依赖提示 (Ubuntu/Debian):
# apt-get install -y libavformat-dev libavcodec-dev libavdevice-dev libavutil-dev libswscale-dev libswresample-dev libavfilter-dev pkg-config

# --- 核心深度学习框架 ---
# 注意：基于 Python 3.12 兼容性，这里使用 >= 2.2.0
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0

# --- EchoMimic 核心依赖 & 兼容性锁 ---
numpy<2.0.0
diffusers==0.24.0
transformers>=4.38.1,<4.41.0
huggingface-hub<=0.25.0
omegaconf==2.3.0
einops==0.4.1

# --- 视觉与多媒体处理 ---
opencv-python
matplotlib
mediapipe>=0.10.13
moviepy==1.0.3
facenet-pytorch==2.5.0
ffmpeg-python
av

# --- 文本与 NLP (Step 1 视素转换) ---
g2p_en
nltk

# --- 工具与 UI ---
accelerate
gradio
lpips
